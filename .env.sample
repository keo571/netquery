# Sample Database Configuration
# This is the default sample/demo database configuration

# ============================================================================
# Schema Configuration
# ============================================================================
# Schema identifier - used for cache isolation and namespace
SCHEMA_ID=sample

# Database connection URL
DATABASE_URL=sqlite:///data/sample.db

# Canonical schema path (generated from Excel via schema ingestion)
CANONICAL_SCHEMA_PATH=schema_files/sample_schema.json

# Excel schema source (for reference)
EXCEL_SCHEMA_PATH=schema_files/sample_schema.xlsx

# ============================================================================
# LLM Configuration (Required)
# ============================================================================
GEMINI_API_KEY= your_gemini_api_key_here

# ============================================================================
# Cache Configuration (Automatic)
# ============================================================================
# Cache files are automatically derived from SCHEMA_ID:
# - Embeddings: data/sample_embeddings_cache.db
# - SQL Cache: data/sample_sql_cache.db

# ============================================================================
# Logging
# ============================================================================
LOG_LEVEL=INFO

# ============================================================================
# CORS Configuration (Optional)
# ============================================================================
# Comma-separated list of allowed origins for CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# ============================================================================
# Setup Instructions
# ============================================================================
# The sample database is pre-configured and ready to use.
#
# To set up or rebuild the sample database:
#
# 1. Create sample database with demo data:
#    python scripts/create_sample_data.py
#
# 2. Build schema embeddings (requires Excel with suggested_queries sheet):
#    python -m src.schema_ingestion build \
#      --schema-id sample \
#      --excel-path schema_files/sample_schema.xlsx \
#      --output-path schema_files/sample_schema.json
#
# Note: Excel schema must include 3 required sheets:
#    - table_schema (REQUIRED): table/column definitions with descriptions
#    - mapping (REQUIRED): foreign key relationships
#    - suggested_queries (REQUIRED): visualization-focused query suggestions
#
# 3. Start backend on port 8000:
#    SCHEMA_ID=sample python -m src.api.server --port 8000
#
# Or use the setup script (for CLI):
#    ./setup-cli.sh
#
# Or use environment file directly:
#    cp .env.sample .env && python -m src.api.server --port 8000

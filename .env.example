# SQLite Configuration (Development)
# Simple file-based database, no server needed
# Perfect for: Quick testing, local development, learning

# ============================================================================
# Database Configuration - Multi-Database Support
# ============================================================================
# Environment setting
NETQUERY_ENV=dev

# Default database (sample is the primary database)
DATABASE_URL=sqlite:///data/sample.db

# Database-specific URLs for dev environment (SQLite)
DATABASE_URL_DEV_SAMPLE=sqlite:///data/sample.db
# DATABASE_URL_DEV_NEILA=sqlite:///data/neila.db  # Uncomment when ready

# ============================================================================
# LLM Configuration (Required)
# ============================================================================
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================================================
# Cache Configuration (Per-Database Isolation)
# ============================================================================
# Cache files automatically derived from SCHEMA_ID
# For SCHEMA_ID=sample: → data/sample_embeddings_cache.db, data/sample_sql_cache.db
# NOTE: Frontend can override this value - this is just the default
SCHEMA_ID=sample

# ============================================================================
# Schema Configuration
# ============================================================================
EXCEL_SCHEMA_PATH=schema_files/sample_schema.xlsx
CANONICAL_SCHEMA_PATH=schema_files/sample_schema.json

# ============================================================================
# Logging
# ============================================================================
LOG_LEVEL=INFO

# ============================================================================
# Usage
# ============================================================================
# 1. Create sample data:   python scripts/create_sample_data.py
# 2. Build schema:         python -m src.schema_ingestion build --excel-path schema_files/sample_schema.xlsx --output-path schema_files/sample_schema.json --schema-id sample
# 3. Query:                python gemini_cli.py "Show me all load balancers"
